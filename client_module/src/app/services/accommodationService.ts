import { accommodation } from '../models/accommodation';
import { Http, Response, Headers, RequestOptions } from '@angular/http';
export class accommodationService{
    public accommodations:Response;
    public accId:number;
}